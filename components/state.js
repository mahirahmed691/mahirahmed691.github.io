// state.js
// Define global state objects shared across your app

export let state = {}; // { category: [items...] }
export let checkedItems = {}; // { itemName: true }
export let dueDates = {}; // { itemName: "YYYY-MM-DD" }
export let collapsedState = {}; // { category: true/false }
